// lib/visual_compare.ts
// Placeholder for future visual compare (disabled).
export type BBox = { x: number; y: number; width: number; height: number };
export type ROI = { bbox: BBox; path?: string };

// In the future we can bring back dhash/hamming, ROI detection, etc.
